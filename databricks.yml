# This is a Databricks asset bundle definition for dlt_meta_dabs.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dlt_meta_dabs
  uuid: e00c37f4-3484-4b2c-9939-7550989039ec

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-e0d9a449-b7e7.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-e0d9a449-b7e7.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/harsha.pasala@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/harsha.pasala@databricks.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: harsha.pasala@databricks.com
        level: CAN_MANAGE
